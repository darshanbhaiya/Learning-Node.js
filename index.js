const lovish =require("./second.js"); // it is in file system , same directory as index.js there fore ./
// remove "type":"module", from package.json to run this as it supports only ES6

console.log("Hello World",lovish)

